import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  spinnerStyle: {
    // Viettel
    // zIndex: 9999,
    // position: 'absolute',
    top: '45%',
    left: '45%',
    position: 'absolute',
    zIndex: 9999,
    // justifyContent: 'center',
    // alignItems: 'center'
  }
})
